import 'package:flutter/material.dart';
// import 'package:flutter_application_2/asset';

void main() {
  runApp(
    MaterialApp(
      home: Scaffold(
        body: Container(
          decoration: const BoxDecoration(
            image: Image(image: ),
            gradient: LinearGradient(colors: [Colors.deepPurple, Colors.cyan],begin: Alignment.topLeft,end: Alignment.bottomCenter),
          ),
        ),
      ),
    ),
  );
}
